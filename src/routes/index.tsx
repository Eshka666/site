import { component$ } from "@builder.io/qwik";

export default component$(() => {
  return (
    <div class="text-center text-gray-700 mt-8">
      <h2 class="text-2xl font-bold">Добро пожаловать!</h2>
      <p>Выберите товар в меню слева.</p>
    </div>
  );
});
